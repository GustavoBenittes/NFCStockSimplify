# Exemplo de arquivo de configuração
# Copie este arquivo para api.config.js e ajuste com seus valores

const API_CONFIG = {
  # Altere para o IP do seu servidor MySQL
  BASE_URL: 'http://192.168.0.100:3000/api',
  
  # Timeout para requisições (ms)
  TIMEOUT: 10000,
  
  ENDPOINTS: {
    # Autenticação
    LOGIN: '/auth/login',
    USERS: '/usuarios',
    
    # Produtos
    PRODUCTS: '/produtos',
    PRODUCT_BY_CODE: '/produtos/codigo/:codigo',
    
    # Movimentações
    MOVEMENTS: '/movimentacoes',
    MOVEMENTS_SYNC: '/movimentacoes/sync',
    
    # Tags NFC
    NFC_TAGS: '/tags-nfc',
    NFC_TAG_BY_VALUE: '/tags-nfc/tag/:valor',
    
    # Sincronização
    SYNC_PULL: '/sync/pull',
    SYNC_PUSH: '/sync/push',
  },
};

export default API_CONFIG;
